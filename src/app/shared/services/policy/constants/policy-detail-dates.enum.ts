/**
 * Enum creada para tener las fechas que seran mostradas segun las necesidades
 */

 export enum PolicyDetailDates {
    RENEWALS_DATE = 101,
    APPLICATION_RECEIVED = 102,
    POLICY_ISSUE_DATE = 100,
}
