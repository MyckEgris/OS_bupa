/***
 * Dto created for mapped the languaje of the subjects title and message
 */
export interface SubjectsByLanguajeDto {
    subjectLanguageId: number;
    subjectTitle: string;
    subjectMessage: string;
}
