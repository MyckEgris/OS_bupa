/*
 * This enum containt the identification types.
 */
export enum IdentificationTypes {
    RFC = 106,
    CURP = 107,
    SERIAL_NUMBER = 108,
    PASSPORTS = 2,
    DOCUMENT = 4,
    REGISTRATION_CERTIFICATE = 113
}
